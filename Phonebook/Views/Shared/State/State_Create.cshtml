@model PhoneBookLib.Business.BState
@using Repository.Business.Protocols
@using Mailbox.Helpers

@{Model.Format();}

@using(Html.BeginForm("State_Create", "Home", FormMethod.Post, 
    new{ 
        @id = "StateCreate_" + Model.State_ID,
        @class="StateCreate PartialUpdateForm"
    })){
    @Model.Country_ID_Rules.Create.HtmlInput(Model.Country_ID)
    @Model.State_ID_Rules.Create.HtmlInput(Model.State_ID)
    @Model.Place_ID_Rules.Create.HtmlInput(Model.Place_ID)
    @Model.Long_Name_Rules.Create.HtmlInput(Model.Long_Name)
    @Model.Short_Name_Rules.Create.HtmlInput(Model.Short_Name)
    
    <input type="submit" value="Create State" />
}